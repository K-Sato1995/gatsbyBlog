import React from 'react'
import Particles from 'react-particles-js'
import { particleOptions, particleStyle } from './Options'

const Index = () => {
  return <Particles style={particleStyle} params={particleOptions} />
}

export default Index
